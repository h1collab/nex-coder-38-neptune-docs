
(()=>{const D=window.ZE;if(!D)return;const list=document.querySelector("[data-list]"),q=document.querySelector("[data-q]"),g=document.querySelector("[data-g]"),chips=[...document.querySelectorAll("[data-s]")],count=document.querySelector("[data-n]");let subject="all";const esc=s=>String(s??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");function card(i){return `<a class="card" href="${esc(i.path)}"><div class="thumb"><img src="${esc(i.illustration)}" alt="${esc(i.title)} illustration" loading="lazy"></div><div class="body"><div class="meta"><span>${esc(i.gradeLabel)}</span><strong>${esc(i.subject)}</strong></div><h3>${esc(i.title)}</h3><p>${esc(i.summary)}</p><div class="badges">${(i.focus||[]).map(x=>`<span class="badge">${esc(x)}</span>`).join("")}</div><div class="arrow">→</div></div></a>`}function animate(){const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("show");obs.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll(".card").forEach(c=>obs.observe(c))}function render(){const query=(q?.value||"").trim().toLowerCase(),grade=g?.value||"all";const filtered=D.items.filter(i=>{const hay=`${i.title} ${i.subject} ${i.summary}`.toLowerCase();return(!query||hay.includes(query))&&(grade==="all"||i.grade===grade)&&(subject==="all"||i.subject===subject)});if(count)count.textContent=`${filtered.length} / ${D.total}`;if(!filtered.length){list.innerHTML='<div class="empty">Nessun materiale corrisponde ai filtri.</div>';return}const groups=new Map();filtered.forEach(i=>{if(!groups.has(i.subject))groups.set(i.subject,[]);groups.get(i.subject).push(i)});list.innerHTML=[...groups.entries()].map(([name,arr])=>`<section class="sb"><div class="sh"><div class="sn">${esc(name)}</div><div class="sd">${esc((arr[0].focus||[]).join(" · "))}</div></div><div class="grid">${arr.map(card).join("")}</div></section>`).join("");animate()}q?.addEventListener("input",render);g?.addEventListener("change",render);chips.forEach(ch=>ch.addEventListener("click",()=>{subject=ch.dataset.s;chips.forEach(x=>x.classList.toggle("on",x===ch));render()}));render()})();
